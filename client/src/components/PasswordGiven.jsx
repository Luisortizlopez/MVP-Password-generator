//This file will be used to capture password generated from (GeneratePassword)
import React from 'react';

const PasswordGiven = (props) => (
  <div>
    <span>{props.i.randompassword}</span>
  </div>
)

export default PasswordGiven;
